interface:
  display_name: "Frontier Independent Review"
  short_description: "Find evidence-backed defect candidates without inventing scope"
  default_prompt: "Use $frontier-independent-review to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: false
