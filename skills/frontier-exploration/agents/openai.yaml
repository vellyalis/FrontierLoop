interface:
  display_name: "Frontier Exploration"
  short_description: "Compare causal hypotheses only under real uncertainty"
  default_prompt: "Use $frontier-exploration to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: false
