interface:
  display_name: "Frontier Routine Change"
  short_description: "Ship clear local changes with the smallest valid diff"
  default_prompt: "Use $frontier-routine-change to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: false
