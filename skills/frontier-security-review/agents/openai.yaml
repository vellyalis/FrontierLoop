interface:
  display_name: "Frontier Security Review"
  short_description: "Protect secrets, permissions, data, and trust boundaries"
  default_prompt: "Use $frontier-security-review to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: true
