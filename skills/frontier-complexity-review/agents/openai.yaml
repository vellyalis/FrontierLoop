interface:
  display_name: "Frontier Complexity Review"
  short_description: "Reject unjustified dependencies and abstractions"
  default_prompt: "Use $frontier-complexity-review to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: false
