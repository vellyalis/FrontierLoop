interface:
  display_name: "Frontier Goal Compiler"
  short_description: "Turn vague requests into observable acceptance"
  default_prompt: "Use $frontier-goal-compiler to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: false
