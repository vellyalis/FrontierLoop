interface:
  display_name: "Frontier Debug Investigation"
  short_description: "Find root cause with discriminating experiments"
  default_prompt: "Use $frontier-debug-investigation to apply this workflow only when its concrete trigger is present."
policy:
  allow_implicit_invocation: true
